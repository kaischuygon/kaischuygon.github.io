---
interface Props {
    hover?: boolean;
}

const { hover=false } = Astro.props;
---

<div class:list={['flex justify-center items-center p-2 gap-1 brutalist-outline-accent bg-base-200 text-base-content font-code', hover ? 'brutalist-outline-hover' : '']}>
    <slot />
</div>
